# PTDoc 开发设计

> 版本：v1.1
> 依据：`需求描述.md`（v1.0，FR-01 ~ FR-06）
> 说明：设计落到具体文件 / 表结构 / 接口签名，作为实施蓝图；未标注"已落地"的条目为待实施项。

## 0. 落地状态

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| FR-01 多文档工作区与文档树 | ✅ M1 已落地 | sidebar 文档树 / rename / delete / 互链站内跳转 |
| FR-03 预览区目录 TOC | ✅ M1 已落地 | slug 锚点 + toc 面板 + 滚动高亮 |
| FR-06 版本快照与回滚 | ✅ M1 已落地 | doc_versions 表 + 4 个接口 + 快照抽屉 |
| FR-02 静态站点发布 | ✅ M2 已落地 | scripts/build-site.ts + MiniSearch + preview-site |
| FR-05 托管链接 / 下载 HTML | ✅ M2 已落地 | 七牛上传 + 回写 + 下载 HTML 按钮 + `GET /s/:id` 托管路由 |
| FR-04 媒体库 | ✅ M3 已落地 | media 表 + 上传落库（X-Source）+ 抽屉面板 + 删除（BucketManager） |

## 1. 现状基线（已核实）

| 能力 | 现状 |
| --- | --- |
| 渲染 | `src/core/markdown.ts`：marked，纯 JS，Node 可跑；`src/core/mermaid.ts`：`renderMermaidToSvg` **Node 兼容（已验证）**，`renderMermaidToPngBlob` / `sha256Mermaid` 依赖浏览器 |
| 服务端 | `db.ts`（docs / shares / images / mermaid_cache 四表 + CRUD）、`api.ts`（/api/docs、/api/share）、`upload-api.ts`（/api/upload，支持 `X-Key-Prefix`）、`qiniu-uploader.ts` |
| 前端 | `app.ts`（编辑器 / 预览 / 上传 / 分享 / 历史）、`fileHandles.ts`（IndexedDB 文件句柄） |
| 分享 | 分享版 mermaid→PNG→七牛、`mermaid_cache` 去重、自包含 HTML 上传七牛 `share/` 前缀 + 永久链接回写 `shares.html_url`（**已落地**） |

**设计原则**：不引入重框架；新增能力拆独立模块（`src/ui/*`、`src/server/*`），复用现有胶水；`doc_key` 语义升级为路径但不改列结构，历史数据零迁移。

---

## 1. FR-01 多文档工作区与侧边栏目录树（P0，M）

### 数据模型
- `docs.doc_key` 列不变，**语义升级为路径**（如 `notes/架构设计.md`）。历史纯文件名数据视为根目录文件，无需迁移。
- 不新增 `parent` 列：路径树由 doc_key 派生，避免冗余与一致性问题（1000 篇内无性能压力）。

### 接口（`api.ts` + `db.ts`）
```
GET  /api/docs?q=          # 改：无 q 时返回全部（原 listRecent 仅 50 条），文档树依赖全量
POST /api/docs             # 复用：新建传 doc_key=路径 即可
POST /api/docs/:id/rename  # 新增 { new_key }：事务更新 docs.doc_key + 级联 shares.doc_key
DELETE /api/docs/:id       # 新增：事务删除 docs + 该 doc_key 的 shares + 关联 images + doc_versions
```

### 前端
- 新文件 `src/ui/sidebar.ts`：文档树——全量 docs 按 `/` 分段构建折叠树，节点显示标题（首级 `#` 或文件名），当前文档高亮；新建 / 重命名 / 删除（二次确认）/ 顶部过滤框（复用 `GET /api/docs?q=`）。
- `app.ts` 布局调整：左侧插入侧边栏；现有历史抽屉保留（定位为"最近打开"）。
- **文档互链**：渲染后拦截 preview 内 `a[href$=".md"]` 点击，由 `当前doc_key目录 + 相对路径` 计算目标 doc_key → 在 docs 全量中定位 → `loadContent` 站内打开（不新开标签）。目录解析 = `dirname(doc_key)` + 归一化（支持 `../`）。
- `fileHandles.ts` 新增 `removeHandle(docKey)`：重命名成功后删除旧键句柄，回退到 DB 内容打开。

### 验收对照
新建 3 篇分属 2 个文件夹 → 两级折叠树；点击即开、自动保存、刷新后仍在；`[](./other.md)` 站内跳转；重命名 / 删除后树与库一致。

---

## 2. FR-03 预览区自动目录 TOC（P0，S）

- 新文件 `src/core/slug.ts`：标题文本 → 稳定 slug（确定性，冲突追加 `-2/-3`）。TOC 与 FR-02 站点共用。
- 渲染时给 preview 内 `h1~h3` 注入锚点 `id`（`app.ts` 的 `render()` 统一处理，与 TOC 同源，防抖并入现有 200ms 渲染周期）。
- 新文件 `src/ui/toc.ts`：渲染后提取标题生成可点击目录，点击平滑滚动并高亮当前项，编辑后自动更新。
- 顶栏 TOC 开关按钮；样式入 `styles.css`。无后端改动。

---

## 3. FR-06 版本快照与回滚（P1，M）

### 数据模型（`db.ts`）
```
doc_versions (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  doc_id     INTEGER NOT NULL REFERENCES docs(id),
  content    TEXT NOT NULL,
  label      TEXT,
  created_at INTEGER NOT NULL
)
```

### 接口
```
POST   /api/docs/:id/snapshots                { label? }  # 基于当前 docs.content 存快照
GET    /api/docs/:id/snapshots                            # 时间倒序列表
POST   /api/docs/:id/snapshots/:sid/restore               # 快照内容写回 docs.content
DELETE /api/docs/:id/snapshots/:sid
```

### 前端
- 顶栏「保存快照」按钮（可填标签）；
- 文档菜单 / 历史抽屉内展示该文档快照列表：预览（只读）、恢复（二次确认；恢复前**自动存一个"恢复前"快照**兜底）、删除。
- 自动保存（`docs.content` 工作副本）与快照（不可变记录）解耦。

---

## 4. FR-02 静态站点发布 build-site（P0，L）

**关键技术点（已验证）**：`renderMermaidToSvg` Node 端直接输出 SVG ✓；`marked.parse` 纯 JS ✓。纯 Node 脚本，运行期零新增依赖（仅 `minisearch` 做搜索）。

### 新增 `scripts/build-site.ts`
```
输入：docs 表全部文档
每篇：renderMarkdown(md) → mermaid 块替换为内联 SVG（复用 renderMermaidToSvg；需 export DIAGRAM_THEME）
      → 站内互链 .md 链接改写为对应 .html 相对链接
      → 写入 dist-site/<doc_key 去 .md>.html（doc_key=index.md → index.html）
骨架：dist-site/index.html 左侧文档树导航 + 顶部搜索框
      MiniSearch 客户端索引：构建期生成 index.json，页面懒加载
     每页注入小标题锚点逻辑（与 FR-03 共用 src/core/slug.ts）
```
- 读库：`initDB()` + 现有 `db.ts` 查询函数（脚本在项目根运行，`process.cwd()` 一致）。
- `package.json`：`"build-site": "node --experimental-strip-types scripts/build-site.ts"`（Node 24 直接跑 TS）、`"preview-site": "vite preview --outDir dist-site"`。
- 新依赖：`minisearch`。

### 验收对照
`dist-site/` 含 index.html + 各文档页 + 索引；预览可切文档、图表为 SVG、互链可跳；搜索命中定位；整目录可拷到任意静态服务器。

---

## 5. FR-05 分享为托管链接或 HTML（P1，M/L）

**已落地**：自包含 HTML 上传七牛 `share/` 前缀 + 永久链接回写 `shares.html_url`。剩余两块：

1. **「下载 HTML」按钮**：`buildShareHtmlDoc` 从 `app.ts` 抽到 `src/core/share-html.ts`（纯函数，前后端共用），分享弹窗加下载按钮。
2. **托管路由 `GET /s/:id`**（可选）：`api.ts` 加路由——读 `shares` 记录 → `buildShareHtmlDoc(share_md)` 渲染返回 HTML 页。链接 = `location.origin + '/s/' + id`。边界：仅 dev/preview 可用；公网永久链接以七牛方案为主（与需求原文分工一致）。

---

## 6. FR-04 媒体库面板（P1，M）

### 数据模型（`db.ts`）
```
media (id PK, qiniu_key TEXT NOT NULL, url TEXT NOT NULL, filename TEXT,
       source TEXT NOT NULL DEFAULT 'direct',   -- 'direct' | 'mermaid'
       size INTEGER, created_at INTEGER NOT NULL)
```

### 接口
```
POST /api/upload       # 改：读 X-Source 头（direct|mermaid），成功后写 media 表
GET    /api/media?q=&source=   # 倒序列表
DELETE /api/media/:id          # 服务端 qiniu.rs.BucketManager.delete(bucket,key) + 删记录，失败明确报错
```
- 去重天然成立：`mermaid_cache` 命中的分享版图片不走 `/api/upload`，不重复写 `media`。
- 前端 `generateShare` 上传调用加 `X-Source: mermaid` 头。

### 前端
- 新文件 `src/ui/media.ts`：顶栏「媒体库」按钮 → 抽屉，缩略图网格 + 来源标签 + 时间；复制 URL、插入 `![](url)` 到光标、删除（二次确认 + 提示"已生成分享版可能需重新生成"）。

---

## 7. 实施顺序（对应需求里程碑）

| 阶段 | 内容 | 说明 |
| --- | --- | --- |
| **M1** | FR-03 TOC（S）→ FR-01 工作区（M）→ FR-06 快照（M） | 让工具"可用、可管理" |
| **M2** | FR-02 build-site（L）→ FR-05 补完（下载 HTML + `/s/:id`） | 让工具"可展示、可分享" |
| **M3** | FR-04 媒体库（M） | 收尾图床体验闭环 |

> 若只先做一件：**FR-01 + FR-03** 以最小成本把"单文件编辑器"升级为"可导航的工作区"，且 FR-02 / FR-05 都依赖其路径语义。

---

## 8. 风险与对策

| 风险 | 对策 |
| --- | --- |
| `doc_key` 改路径后 IndexedDB 句柄按旧键失效 | rename 成功后删除旧键句柄（`fileHandles.removeHandle`），回退 DB 内容打开 |
| 互链相对路径含 `../` 越出工作区 | 归一化后若目标不在 docs 全量，回退普通链接（新标签）并状态栏提示 |
| 媒体库删除不回溯已生成 md | 删除前二次确认明确提示；只删图床对象本身，不动已生成 md |
| build-site 搜索索引随文档数膨胀 | 索引 JSON 独立文件懒加载；50 篇量级无压力 |
| rename 级联 shares 引用 | 与 docs 更新放同一 SQLite 事务，保证一致性 |
