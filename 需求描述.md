# 需求描述：PTDoc 增强（P0 / P1）

> 版本：v1.0
> 适用范围：在现有 PTDoc（Markdown + Mermaid + 七牛图床 + 分享版 + 历史）基础上，补齐"多文档工作区、静态站点发布、TOC、媒体库、托管分享、版本快照"六项为 P0/P1 需求。
> 现有能力基线：
> - 编辑/预览：左侧写 Markdown，右侧实时渲染；` ```mermaid ` 由 `beautiful-mermaid` 渲染为 SVG。
> - 图床：浏览器把图片 POST 到 dev/preview 服务端 `/api/upload`，由 `qiniu` SDK 上传七牛，返回 `{ url, key }`（现为公开桶，URL 永久有效）。
> - 分享版：把 mermaid 渲染成 PNG 上传七牛，替换为正文远程图片链接；元信息存入 `shares`/`images` 表。
> - 历史：打开/编辑过的 md 全文存入 `docs` 表，可搜索重开。
> - 去重：`mermaid_cache` 表按 mermaid 内容 SHA-256 缓存已上传图片 URL。

---

## 0. 背景与目标

当前 PTDoc 已跑通"本地 Mermaid 编写 → 一键转远程图分享 → 去重缓存 → 历史"的核心闭环，但本质是**单文件智能编辑器**：一次只能编辑一个文件、文档间无结构、对外只能复制 `.md` 文本。

**总体目标**：把它升级为"个人文档站"——既能多文档结构化管理与浏览（展示），又能一键发布/分享成他人可直接查看的成品，并把图床素材沉淀为可管理的媒体库。

**成功指标（建议）**：
- 用户可在工作区内维护 ≥ 10 篇互链文档并顺畅导航（P0-1）。
- 一键产出可部署的自包含静态站点，含目录与搜索（P0-2）。
- 分享/发布产物中，读者无需任何 Markdown/Mermaid 环境即可看到完整图文（P0-2 / P1-5）。
- 用户对图床素材有可见、可管理的视图（P1-4）。

---

## 1. 需求总览（优先级）

| 编号 | 需求 | 优先级 | 价值 | 工作量 | 依赖 |
| --- | --- | --- | --- | --- | --- |
| FR-01 | 多文档工作区与侧边栏目录树 | P0 | 高 | M | 现有 `docs` 表 |
| FR-02 | 静态站点发布（build-site） | P0 | 高 | L | FR-01、分享版渲染逻辑 |
| FR-03 | 预览区自动目录 TOC | P0 | 中 | S | 现有渲染管线 |
| FR-04 | 媒体库面板 | P1 | 高 | M | 需新增 `media` 表与 `/api/media` |
| FR-05 | 分享为托管链接或 HTML | P1 | 高 | M/L | 分享版渲染、FR-02 渲染器 |
| FR-06 | 版本快照与回滚 | P1 | 中 | M | 现有 `docs` 表 |

---

## 2. P0 需求详述

### FR-01 多文档工作区与侧边栏目录树

**目标**：支持在一个工作区内管理多篇 Markdown 文档（可分层级/文件夹），提供侧边栏文档树与跨文档导航，替代当前"单编辑器 + 扁平历史列表"。

**用户故事**：
- 作为作者，我希望在左侧看到我所有文档的树状列表，点击即可打开，而不必每次用"打开 .md"重新选文件。
- 作为作者，我希望新建/重命名/删除文档，并支持按文件夹归类。
- 作为读者，我希望在文档内点击链接能跳到另一篇文档（相对路径互链）。

**功能需求**：
1. 文档标识支持"路径"语义：`doc_key` 由纯文件名升级为路径（如 `notes/架构设计.md`），用于构建树。
2. 侧边栏文档树：
   - 按路径层级渲染为可折叠树；显示标题（取自首级 `#` 或文件名）。
   - 支持"新建文档""重命名""删除"（删除需二次确认，仅删本地记录，不删磁盘原文件）。
   - 搜索框可过滤树（复用 `GET /api/docs?q=`）。
3. 打开文档：点击树节点 → `GET /api/docs/:id` 加载全文到编辑器，并写入"当前文档"上下文。
4. 新建/保存：编辑器变更经自动保存写回对应 `doc_key`；新建时生成新 `doc_key` 并落入 `docs` 表。
5. 文档互链：预览区内 `[](相对路径.md)` 点击时，在工作区内打开目标文档（不再新开浏览器标签）。
6. 当前文档高亮：树中标记正在编辑的文档。

**非功能需求**：
- 树渲染在 1000 篇文档内无明显卡顿（前端做简单分组，无需虚拟列表）。
- 文档树状态随增删改实时刷新。

**数据 / 接口变更**：
- `docs` 表：`doc_key` 语义扩展为路径（唯一约束不变）；建议新增 `parent TEXT`（父目录，用于树构建，可派生，属可选）。
- 新增接口：
  - `POST /api/docs/:id/rename` `{ new_key }` → 更新 `doc_key`（并级联更新 `shares.doc_key` 引用，视情况）。
  - `DELETE /api/docs/:id` → 删除文档记录。
  - `POST /api/docs` 已存在，复用；新增文档时 `doc_key` 为路径即可。
- 互链解析在前端完成（基于当前文档 `doc_key` 的目录前缀解析相对路径）。

**验收标准**：
- 新建 3 篇分属两个文件夹的文档，侧边栏正确显示两级树并可折叠。
- 点击文档即在编辑器打开；编辑后自动保存，刷新页面（重开工作区）后内容仍在。
- 文档内 `[](./other.md)` 点击可在工作区内跳转，不新开标签。
- 重命名/删除后树与数据库一致。

**范围边界**：
- 不实现磁盘文件双向同步（仍仅本地 DB 为权威）；不实现多人协作/权限。
- 文件夹为逻辑分组，不直接映射宿主机目录（除非后续接入 File System Access API）。

---

### FR-02 静态站点发布（build-site）

**目标**：把工作区（或 `public/docs/`）中的全部 Markdown 渲染为**自包含静态站点**，可直接部署到任意静态托管，实现"展示 md 文档"的对外落地。

**用户故事**：
- 作为作者，我希望一条命令把我的所有文档导出成网站（HTML），发给同事或部署到内网/公网，对方打开即是带目录、可搜索的文档站，图片与图表都正常显示。
- 作为读者，我希望在站点内通过左侧导航和搜索浏览所有文档，图表是图不是源码。

**功能需求**：
1. 新增 `npm run build-site` 命令（Node 脚本，如 `scripts/build-site.ts`，复用 `marked` + `beautiful-mermaid` + 现有分享版渲染逻辑）。
2. 渲染规则：
   - 每篇文档渲染为独立 HTML 页；` ```mermaid ` 渲染为**内联 SVG**（与编辑预览一致，自包含、无需图床也可显示）。
   - 文档中已存在的远程图片 URL（含分享版生成的七牛图）原样保留。
   - 直接图床上传的图片若仍引用本地相对路径，则一并拷贝/上传并保持引用。
3. 站点骨架：
   - 首页/导航：左侧文档树（由 `docs` 路径派生），右侧内容；顶部搜索框。
   - 每页含自动生成的小标题锚点（与 FR-03 TOC 共用逻辑）。
   - 站内文档互链保留为相对 HTML 链接。
4. 搜索：构建期生成全文索引（如 MiniSearch），站点内客户端模糊搜索（纯静态、无后端）。
5. 产物输出到 `dist-site/`，可直接 `npm run preview-site` 或部署。

**非功能需求**：
- 产物纯静态、可离线打开（除远程图片需网络）。
- 构建 50 篇文档在数秒内完成。

**数据 / 接口变更**：
- 无后端接口（构建期直接读 `docs` 表或 `public/docs/`）。
- 复用：`renderMarkdown`（`src/core/markdown.ts`）、`renderMermaidBlocks`（`src/core/mermaid.ts`）、分享版的"mermaid→远程图"逻辑（如需把图表也走图床而非内联 SVG，可复用 `renderMermaidToPngBlob` + 上传）。
- 新增 `scripts/build-site.ts` 及 `vite` 之外的轻量构建入口（或加 `vite` 模式）。

**验收标准**：
- `npm run build-site` 后 `dist-site/` 含 `index.html` + 各文档页 + 索引。
- 用 `npm run preview-site`（静态服务器）打开，左侧导航可切换文档，图表显示为 SVG，文档内互链可跳转。
- 搜索框输入关键词能命中并定位文档。
- 站点可整体拷贝到任意静态服务器访问。

**范围边界**：
- 托管链接/服务端渲染不在本需求（见 FR-05）。
- 不处理鉴权；公开文档站默认无访问控制。

---

### FR-03 预览区自动目录 TOC

**目标**：在预览区（或独立浮层）根据文档标题自动生成目录大纲，支持点击跳转，提升长文档可读性。

**用户故事**：
- 作为读者/作者，我希望在长文档右侧/左上看到大纲，点标题即滚到对应位置。

**功能需求**：
1. 解析当前渲染后的预览 DOM，提取 `h1~h3`（可配置层级）生成有序大纲。
2. 为每个标题注入稳定锚点 `id`（基于标题文本 slug，冲突时追加序号）。
3. TOC 展示为可点击列表，点击平滑滚动到对应标题并高亮。
4. 编辑内容变更后 TOC 自动更新（与渲染同周期防抖）。
5. 提供显示/隐藏 TOC 的开关（顶栏按钮或预览区浮层）。

**非功能需求**：
- TOC 生成开销可忽略；锚点 `id` 在重新渲染后保持稳定（避免点击后即跳错）。

**数据 / 接口变更**：无（纯前端）。

**验收标准**：
- 打开含多级标题的文档，TOC 列出 h1~h3 且层级缩进正确。
- 点击 TOC 项，预览滚动到该标题并高亮；编辑后 TOC 同步更新。
- 锚点 `id` 在重新渲染后保持稳定（避免点击后即跳错）。

**范围边界**：不实现"文档级大纲导出为独立页"（那是 FR-02 的一部分）。

---

## 3. P1 需求详述

### FR-04 媒体库面板

**目标**：把"图床"沉淀为可管理的素材库——集中展示所有上传过的图片（含直接上传与分享版 mermaid 图），支持复制 URL、删除、插入，解决"图片一插进 md 就消失"的体验断层。

**用户故事**：
- 作为作者，我希望在一个面板里看到我所有传过的图，复制某张图的 URL 或把它再插入到别处，也能删掉不再需要的图。
- 作为作者，我希望知道哪些图是 mermaid 生成的、哪些是随手传的。

**功能需求**：
1. 落库所有上传：扩展上传链路，每次 `/api/upload` 成功后写入新的 `media` 表（字段：`id, qiniu_key, url, filename, source('direct'|'mermaid'), size, created_at`），`mermaid_cache` 命中时不重复写。
2. 媒体库面板（顶栏"媒体库"按钮 → 抽屉）：
   - 缩略图网格列表，显示文件名、来源标签、时间。
   - 操作：复制 URL、插入到编辑器光标处（`![](url)`）、删除（调用七牛 `bucketManager.delete`，同时从 `media` 表移除）。
   - 搜索/筛选（按来源、文件名）。
3. 删除需二次确认；删除仅影响图床与媒体库，不回溯修改已生成的 md 文本（提示用户相关文档可能需重新生成分享版）。

**非功能需求**：
- 列表在数千张图内流畅（分页或虚拟滚动可选）。
- 删除操作有失败兜底提示（如七牛网络异常）。

**数据 / 接口变更**：
- 新增表 `media`（见上）。
- `/api/upload`：成功后写 `media` 表。
- 新增接口：
  - `GET /api/media?q=&source=` → 列表。
  - `DELETE /api/media/:id` → 服务端调 `qiniu.rs.BucketManager.delete(bucket, key)` 并删记录。

**验收标准**：
- 上传一张图后，媒体库出现该图（来源=direct）；生成一次分享版后，其 mermaid 图出现在媒体库（来源=mermaid）且不与 `mermaid_cache` 重复写。
- 媒体库"复制 URL"可用；"插入"把 `![](url)` 放到光标处。
- "删除"后七牛桶内文件消失且媒体库不再列出；失败有明确提示。

**范围边界**：
- 不实现图床配额/批量重传；不实现图片编辑/裁剪。
- 删除不做文档级反向清理（仅提示）。

---

### FR-05 分享为托管链接或 HTML

**目标**：把"分享版"从"复制 `.md` 文本"升级为更易消费的成品——可下载的自包含 HTML，或工作区内可访问的托管渲染页，降低接收方门槛。

**用户故事**：
- 作为作者，我希望一键得到"对方打开就是排版好的页面"的成品：要么下载一个 `.html` 文件发微信/邮件，要么得到一个本环境内可访问的链接。
- 作为接收方，我打开链接/HTML 即看到完整图文，无需装任何工具。

**功能需求**：
1. 「生成分享版」在现有产出 `.md` 的基础上，增加两种产出：
   - **下载 HTML**：把分享版 md 用现有渲染管线生成自包含 HTML（内联基础样式、图片为远程 URL、mermaid 已为 PNG/远程图），提供下载。
   - **托管链接**（可选）：把分享记录暴露为服务端路由 `GET /s/:id`，返回渲染后的分享页（复用分享版 md + 基础模板）。链接在本环境（dev/preview）可访问。
2. 分享弹窗新增"下载 HTML""复制链接"按钮（链接在启用托管时可用）。
3. 分享记录（`shares` 表）已存 `share_md`，托管页在请求时实时渲染为 HTML（或构建期预渲染缓存）。

**非功能需求**：
- 生成的 HTML 自包含、可离线查看（除远程图片需网络）。
- 托管链接随 dev/preview 服务端可用；纯静态部署时链接不可用（此时以"下载 HTML"为主，需在 FR-02 中一并产出）。

**数据 / 接口变更**：
- 复用 `shares` 表（已有 `share_md`）。
- 新增路由 `GET /s/:id` → 读 `shares` 记录，渲染为 HTML 页返回（服务端用 `marked` + `beautiful-mermaid` 或直接将 `share_md` 转 HTML；图片已是远程 URL，无需再上传）。
- 前端：分享弹窗增加按钮与"复制链接"逻辑（链接 = `location.origin + '/s/' + id`）。

**验收标准**：
- 点"下载 HTML"得到文件，浏览器打开即看到完整图文排版。
- 启用托管时，"复制链接"得到的 `/s/:id` 在本环境打开即渲染分享页。
- 分享页中图片均来自远程 URL，正常显示。

**范围边界**：
- 托管链接依赖服务端运行（非纯静态）；若需公网永久链接，应纳入 FR-02 的静态发布或单独部署分享服务。
- 不做访问统计/有效期控制（后续可扩展）。

---

### FR-06 版本快照与回滚

**目标**：在现有"自动保存全文到 `docs`"之上，提供显式命名快照与回滚，让"历史"不止是"最后内容"，而是可追溯的版本线。

**用户故事**：
- 作为作者，我希望在关键节点（如"定稿""发给评审前"）存个快照，之后改乱了能一键回到那个版本。
- 作为作者，我希望看到某篇文档的版本列表与时间，并能预览/恢复其中任一版。

**功能需求**：
1. 新增"保存快照"按钮：对当前文档生成一条快照，含可选标签（如"v1 评审稿"），写入 `doc_versions` 表（`id, doc_id, content, label, created_at`）。
2. 历史/文档菜单中展示该文档的快照列表（时间倒序），每项可：预览（载入只读视图）、恢复（覆盖回编辑器当前内容并触发保存）、删除。
3. 自动保存仍持续更新 `docs.content`（当前工作副本）；快照为独立不可变记录，二者解耦。
4. 恢复时给出确认，避免误覆盖当前内容。

**非功能需求**：
- 快照存储体积可控（纯文本，单篇可接受）；可选限制每篇最多 N 个快照或自动清理最旧。
- 恢复操作有撤销提示（或先自动存一个"恢复前"快照）。

**数据 / 接口变更**：
- 新增表 `doc_versions (id, doc_id INTEGER FK docs(id), content TEXT, label TEXT, created_at INTEGER)`。
- 新增接口：
  - `POST /api/docs/:id/snapshots` `{ label? }` → 基于当前 `docs.content` 存快照。
  - `GET /api/docs/:id/snapshots` → 列表。
  - `POST /api/docs/:id/snapshots/:sid/restore` → 将快照内容写回 `docs.content`（可选，也可前端拉取后走现有保存）。
  - `DELETE /api/docs/:id/snapshots/:sid`。

**验收标准**：
- 对文档存 2 个带标签快照，修改当前内容后，从快照列表"恢复"其一，编辑器内容回到该快照，且原"恢复前"内容仍可通过再次快照保留。
- 预览快照为只读，不影响当前编辑。
- 删除快照后列表不再显示，且不影响当前文档内容。

**范围边界**：
- 不做行级 diff/合并；不做跨文档版本。
- 快照仅本地 DB，不跨设备同步。

---

## 4. 依赖与里程碑建议

**依赖关系**：
- FR-02 依赖 FR-01（站点导航需文档树/路径）。
- FR-05 的托管链接可独立于 FR-02，但"公网永久链接"需 FR-02 的静态发布承接。
- FR-04 需先有"所有上传落库"（扩展 `/api/upload`），再提供面板。
- FR-03 无依赖，可最先做。

**建议里程碑（M1→M3）**：
- **M1（Quick wins）**：FR-03 TOC（S） → FR-01 多文档工作区（M） → FR-06 版本快照（M）。先让工具"可用、可管理"。
- **M2（发布能力）**：FR-02 静态站点发布（L） → FR-05 分享为 HTML/托管链接（M/L）。让工具"可展示、可分享成品"。
- **M3（素材闭环）**：FR-04 媒体库（M）。收尾图床体验。

> 若资源只允许先做一件，建议 **FR-01 多文档工作区 + FR-03 TOC**：它们以最小成本把"单文件编辑器"升级为"可导航的个人文档站"，且完全复用现有 SQLite 历史能力。

---

## 5. 风险与开放问题

- **静态站点的 mermaid 呈现**：内联 SVG（自包含、离线可用）与"走图床远程图"两种策略需二选一或都支持；建议默认内联 SVG，分享给外部时可选远程图（见 FR-02 / FR-05）。
- **托管链接的可用性**：`/s/:id` 仅在 dev/preview 服务端运行时可用，公网分享需配合 FR-02 部署；需向用户明确此边界。
- **媒体库删除的副作用**：删除桶内图片不会回溯已生成的 md 文本，需在 UI 明确提示（FR-04）。
- **文档路径语义升级**：`doc_key` 由文件名改为路径，需兼容历史数据迁移（已有的 `doc_key` 视为根目录文件）。
